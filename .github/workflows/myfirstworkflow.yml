name: Print Hello World
on:
    push:
jobs:
    printHelloWorld:
        runs-on: ubuntu-latest
        steps:
            - name: Print Hello World
              run: echo "Hello World"
              